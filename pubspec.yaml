name: japas
description: "Platform Jasa Inklusif"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core
  supabase_flutter: ^2.5.0
  riverpod: ^2.5.0
  flutter_riverpod: ^2.5.0
  riverpod_generator: ^2.4.0
  go_router: ^14.0.0

  # UI & Utility
  google_fonts: ^6.2.1
  intl: ^0.19.0
  flutter_native_splash: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true